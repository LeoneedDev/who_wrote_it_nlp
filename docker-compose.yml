services:
  app:
    build:
      context: .
    ports:
      - "5000:5000"
    environment:
      FLASK_APP: app.py
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
